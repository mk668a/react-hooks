import React, { FunctionComponent } from "react";
import { hooks } from "../constants";
import { Layout } from "../layout/layout";

export const UseImperativeHandle: FunctionComponent = () => {
  return (
    <Layout title={hooks.useImperativeHandle}>
      <></>
    </Layout>
  );
};
