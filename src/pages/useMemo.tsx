import React, { FunctionComponent } from "react";
import { hooks } from "../constants";
import { Layout } from "../layout/layout";

export const UseMemo: FunctionComponent = () => {
  return (
    <Layout title={hooks.useMemo}>
      <></>
    </Layout>
  );
};
