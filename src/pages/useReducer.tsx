import React, { FunctionComponent } from "react";
import { hooks } from "../constants";
import { Layout } from "../layout/layout";

export const UseReducer: FunctionComponent = () => {
  return (
    <Layout title={hooks.useReducer}>
      <></>
    </Layout>
  );
};
