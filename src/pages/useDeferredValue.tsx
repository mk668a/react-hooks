import React, { FunctionComponent } from "react";
import { hooks } from "../constants";
import { Layout } from "../layout/layout";

export const UseDeferredValue: FunctionComponent = () => {
  return (
    <Layout title={hooks.useDeferredValue}>
      <></>
    </Layout>
  );
};
