import React, { FunctionComponent } from "react";
import { hooks } from "../constants";
import { Layout } from "../layout/layout";

export const UseDebugValue: FunctionComponent = () => {
  return (
    <Layout title={hooks.useDebugValue}>
      <></>
    </Layout>
  );
};
