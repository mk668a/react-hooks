import React, { FunctionComponent } from "react";
import { hooks } from "../constants";
import { Layout } from "../layout/layout";

export const UseLayoutEffect: FunctionComponent = () => {
  return (
    <Layout title={hooks.useLayoutEffect}>
      <></>
    </Layout>
  );
};
