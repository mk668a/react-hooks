import React, { FunctionComponent } from "react";
import { hooks } from "../constants";
import { Layout } from "../layout/layout";

export const UseRef: FunctionComponent = () => {
  return (
    <Layout title={hooks.useRef}>
      <></>
    </Layout>
  );
};
